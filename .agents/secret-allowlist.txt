# Sır taraması muafiyet listesi — biçim: yol-deseni  # gerekçe (zorunlu).
# Kapsam kapı bazındadır: işaretsiz satır yalnız secret kapısına uygulanır.

# Postgres bağlantı örneğindeki "auras" parolası — belgelenmiş, yalnız-yerel
# dev/demo değeri (docker-compose.yml POSTGRES_PASSWORD, üretimde .env'den
# değiştirilir). .gitleaks.toml zaten aynı deseni regex allowlist'inde
# taşıyor; burada da aynı gerekçeyle muaf.
.gitleaks.toml  # gitleaks allowlist regex'inin kendisi — desen, sır değil
README.md  # kurulum örneğindeki yerel-dev bağlantı dizesi (auras:auras@localhost)
config.yaml  # yorum satırındaki yerel-dev bağlantı dizesi örneği
docker-compose.yml  # yerel-dev POSTGRES_PASSWORD, üretimde .env'den değiştirilir
setup.sh  # kurulum betiğinin yerel-dev varsayılan bağlantı dizesi
windows/setup.ps1  # Windows kurulum betiğinin yerel-dev varsayılan bağlantı dizesi
