---
title: "Run — POC iskeleti + 3 modül çalışır (count/plate/face)"
type: run
status: completed
date: 2026-06-29
run_id: 2026-06-29T18-29-15-e3c1
topics: [poc, sayma, plaka, yüz]
tags: [run/completed]
related_decisions: [[decisions/0001-yigin-ve-mimari]]
aliases: [run-2026-06-29-poc]
---

# Run — POC iskeleti + 3 modül çalışır

## İstek
Açık kaynak demo videolarla, üç analizi sırayla geliştir (sayım → plaka → yüz → analyze).

## Yapıldı
- ⚡ Backend — `src/` modüler iskelet: cli, config, device(MPS>CUDA>CPU), detect(YOLO singleton), count, plate, face, store(SQLite).
- 🚢 DevOps — venv (Py3.11) + Ultralytics/OpenCV/fast-alpr/onnxruntime/insightface kuruldu. Demo videolar indi (intel-iot-devkit).
- 🔍 QA — syntax + smoke + DB tutarlılık yeşil.

## Sonuçlar (M4 / MPS)
- **Sayım** (people-detection.mp4): IN 2 / OUT 1, 596 kare. YOLO MPS'te. ✅ annotated video.
- **Plaka** (car video): 19 okuma, ~3-25sn. fast-alpr **CPU provider** (CoreML EP dinamik-şekil hatası → CPU'ya zorlandı). OCR gürültülü → çok-kareli oylama gerek.
- **Yüz** (people video): tespit + yaş/cinsiyet + DB çalışıyor; gözetim açısında yüz az (beklenen). InsightFace buffalo_l, CPU.
- **analyze**: üçü birden tek videoda. ✅

## Öğrenilen / Tuzaklar
- fast-alpr Apple Silicon'da CoreML EP ile patlıyor → `detector_providers/ocr_providers=["CPUExecutionProvider"]` şart.
- fast-alpr OCR confidence **liste** dönebilir (karakter bazlı) → ortalama al.
- `lap` ByteTrack için gerekli (Ultralytics autoupdate kuruyor, requirements'a eklendi).
- onnxruntime fast-alpr ile gelmedi, ayrı kuruldu.
- Çizgi geçiş sayımı çizgi konumuna duyarlı (config.count.line) — video başına ayarlanmalı.

## Değişen / Eklenen
- `src/*.py` (8 modül), `config.yaml`, `requirements.txt`, `.gitignore`, `README.md`
- `output/videoai.db` (runs/count_events/plate_reads/face_events), annotated mp4'ler (repoya girmez)

## Sonraki Adım
- [ ] Çok-kareli plaka oylama (aynı track → en güvenilir metin) + TR plaka fine-tune
- [ ] Re-ID / çok-kameralı rota (yüz embedding + plaka metin eşleştirme)
- [ ] Retail: ısı haritası + footfall + anonim demografi dashboard'u
- [ ] Gerçek senaryo videolarıyla eşik kalibrasyonu
