---
title: "Run — Backlog tamamı: plaka oylama, UI kamera, arka plan job, canlı yenileme, yüz watchlist"
type: run
status: completed
date: 2026-06-29
run_id: 2026-06-29T22-22-54-5d99
topics: [ui, plaka, yüz, sayma]
tags: [run/completed]
related_decisions: [[decisions/0001-yigin-ve-mimari]]
aliases: [run-2026-06-29-backlog-hepsi]
---

# Run — "hepsini yap" (yarın test)

## İstek
Kalan tüm backlog tek seferde: yarın test edilecek.

## Yapıldı
- ⚡ **Plaka çok-kareli oylama** (`plate.py`): Levenshtein≤2 kümeleme → küme temsilcisi en sık/güvenilir metin. `PlateResult.voted=[{plate,count,conf}]`. Test: 19 ham okuma → 17 araç (mükerrer ×2 birleşti).
- ⚡ **Arka plan job** (`server.py`): `/api/run` artık thread başlatıp `job_id` döner; `GET /api/run/{job_id}` durum+stage. Uzun video UI'yı kilitlemez. UI ilerleme çubuğuyla poll eder.
- ⚡💾 **Kamera yönetimi**: `cameras` DB tablosu; `_cameras()` config+DB merge; `POST/DELETE /api/cameras`. UI'dan ad+source ile eklenir (RTSP veya yerel video). Test: "Test Kapı" eklendi, restart sonrası canlıda kalıcı.
- ⚡ **Yüz watchlist eşleşme**: `watch_faces.embedding`; `embed_largest_face` (12 örnek kare tarar — gözetimde yüz seyrek); `run_face(watch=...)` cosine≥eşik → match → alert. UI: Listeler'de yüz için kamera seç → enroll. KANIT: enroll→tespit→eşleşme score 1.0.
- 🎨 **Canlı auto-refresh**: 5sn'de bir tile snapshot yeniler (server TTL cache 4sn ile uyumlu).

## Doğrulama
- syntax ✅ · kamera ekle/listele ✅ · job poll (running→done, stage) ✅ · plaka oylama (19→17, ×count) ✅ · yüz enroll(512-d)+match(1.0) ✅ · canlı 4 kamera (Test Kapı kalıcı) screenshot ✅ · cosine(self)=1.0 ✅

## Notlar / tuzaklar
- Yüz/uzun video CPU'da yavaş (kasa 2330 kare >40sn) → arka plan job şart oldu; üretimde GPU.
- Tek-kare enroll güvenilmez → çok-kare tarama. Eşleşme için tespit stride'ı yüz karelerini yakalamalı (stride küçük tut veya conf ayarla).
- DB'de test artıkları var (kamera "test-kap", plaka EBC529, yüz "Test Kişi") — UI'dan ✕ ile silinebilir.

## Değişen
- `src/plate.py` (oylama), `src/face.py` (embed+match), `src/store.py` (cameras+faces embedding+match), `src/server.py` (job+kamera+enroll+eşleşme), `web/index.html` (kamera ekle/sil, job ilerleme, auto-refresh, yüz enroll)

## Sonraki Adım (yarın test sonrası)
- [ ] Gerçek footage ile eşik/çizgi kalibrasyonu (sayım + plaka conf + yüz threshold)
- [ ] TR plaka fine-tune · Re-ID/rota · retail ısı haritası
