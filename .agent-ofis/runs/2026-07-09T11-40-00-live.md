---
title: "Run — Test ekranı canlı analiz görüntüsü (kare + artan sayaç birlikte)"
type: run
run_id: 2026-07-09T11-40-00-live
date: 2026-07-09
topics: [ui, sayma, yüz, plaka, kvkk]
tags: [run/completed, ui]
aliases: [canli-analiz-goruntusu, live-frame]
related_runs: "[[runs/2026-07-09T11-15-00-vdai]]"
---

# Run — 2026-07-09 — Canlı analiz görüntüsü

**Şikâyet:** Test ekranında analiz sırasında yalnız sayılar akıyordu, annotated video iş bitince geliyordu. İstenen: kare + artan değer BİRLİKTE canlı; ekran ilk açılışta eski sayaçlar görünmesin.

## Çözüm mimarisi
- count/face/plate `run_*`'a **`on_frame` callback** (annotated kare; writer'dan bağımsız, imzalar geriye-uyumlu — tüm çağrılar keyword'lü).
- server: `_frame_pusher(job)` → throttle (0.15s) + max 960px + JPEG q80 **yalnız bellekte** → `job["frame_jpeg"]` + `frame_seq`. Yeni `GET /api/run/{id}/frame` (204 boşsa). `api_run_status` `dict(j)` kopya (thread yarışı) + frame_jpeg dışlanır. `finally`'de `frame_jpeg` pop (KVKK: bitince kare bellekte kalmaz). `GET /api/counts` → `{"active", "rows"}`.
- UI: `ensureLivePanel()` (canlı `<img id="liveframe">` + `#tmetrics`), 300ms poll, `frame_seq` değişince img tazelenir (`?seq=N`+token). Metrik renderer'ları `_tm()`→`#tmetrics`. "analyze" stage-prefix'e göre panel. `countBox`/`overviewStats` yalnız `LIVE_ACTIVE` iken dolu (ilk açılışta boş). **Fix:** "Hepsi/analyze" seçilemiyordu (enabled fallback) → analyze her zaman seçilebilir.

## Doğrulama
- py_compile ✓ · pytest 22/22 ✓
- E2E duman (gerçek TRT analiz): frame_seq artıyor, /frame image/jpeg (magic ✓), counts.active doğru, done sonrası /frame **204** ✓
- Bağımsız denetçi (subagent): VETO (3 bulgu: analyze ölü kod, dict yarışı, KVKK kare kalıcılığı) → üçü düzeltildi + e2e ile kanıtlandı.

## Backlog temizliği (kullanıcı onayıyla, aynı gün)
- ✔ `_frame_pusher.push` try/except — canlı önizleme hatası analizi düşüremez (writer sızıntısı tetikleyicisi kapandı)
- ✔ face culling sırası: önce tracks daraltılır, sonra finalize (aktif sayaç doğru)
- ✔ JS null-guard: setRun/setRes + renderer tgt guard + ensureLivePanel — ekran değişiminde poll sessiz sürer
- ~ save_video=False + on_frame'de her karede plot: bilinçli bırakıldı (bugün çağıranı yok, tasarım notu)

## Commit
`be78e17` — 6 dosya, +274/−109 (canlı sayaç + canlı analiz görüntüsü + veto/backlog düzeltmeleri).
Doğrulama: pytest 22/22 · JS parse (node) ✓ · final e2e count (TRT) + face (CUDA, frame_seq 144) ✓

## Not
Değişikliğin görünmesi için sunucu yeniden başlatılmalı: `python -m src.server`.
