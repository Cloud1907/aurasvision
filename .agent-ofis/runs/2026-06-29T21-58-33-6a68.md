---
title: "Run — Premium arayüz: performans (snapshot cache) + tasarım sistemi + dark mode"
type: run
status: completed
date: 2026-06-29
run_id: 2026-06-29T21-58-33-6a68
topics: [ui]
tags: [run/completed]
related_decisions: [[decisions/0001-yigin-ve-mimari]]
aliases: [run-2026-06-29-premium-ui]
---

# Run — Premium arayüz (perf + şıklık)

## İstek
"Arayüz çok basit, premium yap — önce performans sonra şıklık."

## Yapıldı
- ⚡ Backend — `/api/snapshot` **TTL cache** (4 sn) + Cache-Control. VideoCapture her çağrıda açılmıyor → 1. çağrı 44ms, 2. (cache) **1.2ms (~37×)**.
- 🎨 Frontend — `web/index.html` baştan: **tasarım sistemi** (CSS token, light+dark), `<head>` anti-FOUC tema script + localStorage toggle. **Dark sidebar rail** (gradient logo, gruplu nav İZLEME/KAYIT, inline SVG ikon, aktif çubuk). Sticky topbar (başlık+alt). Kartlar/metrikler/chip, kamera tile'ları **hover-lift + fade-in + skeleton shimmer**, segment kontrol, içerik fade geçişi. Web font yok (perf, sistem font stack). Tüm 5 ekran + Test + editör yeniden stillendi, fonksiyon korundu.

## Doğrulama (Claude_Preview)
- Snapshot perf: 1.2ms cache, Cache-Control: max-age=4 ✅
- Canlı: 3 tile gerçek snapshot, fade + CANLI chip ✅
- Test (light): segment + indigo Çalıştır + metrik kart ✅
- Bölgeler (light): canvas + A/B çizgi + kart araç paneli + liste ✅
- Dark ↔ light toggle ✅ (rail her zaman dark — premium görünüm)

## Notlar
- İçerik `fade` animasyonu var → screenshot anlık yakalarsa yarı-saydam görünebilir (kozmetik, ~0.22s).
- Backend yalnız snapshot cache değişti; diğer endpoint sözleşmeleri sabit.

## Değişen
- `src/server.py` — snapshot TTL cache + Cache-Control
- `web/index.html` — komple premium yeniden tasarım (tek dosya, bağımlılık yok)

## Sonraki Adım
- [ ] Watchlist eşleşme alarmı (kara liste → Olaylar kırmızı)
- [ ] Canlı tile'larda periyodik otomatik yenileme (cache TTL ile uyumlu)
- [ ] Uzun video için /api/run arka plan job
