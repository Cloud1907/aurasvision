---
title: "Run — Veri doğruluğu kanıtı + watchlist eşleşme alarmı"
type: run
status: completed
date: 2026-06-29
run_id: 2026-06-29T22-08-41-f9fe
topics: [ui, sayma, plaka, kvkk]
tags: [run/completed]
related_decisions: [[decisions/0001-yigin-ve-mimari]]
aliases: [run-2026-06-29-dogrulama-alarm]
---

# Run — Veri doğruluğu + watchlist alarmı

## İstek
"Önce doğru veri üretiyor mu — o önemli. Diğerlerini de yap. Veri nerede tutulacak/gösterilecek?"

## Veri doğruluğu (kanıt)
- **Yön semantiği** unit-test ile kanıtlandı: editör "A→B" yan etiketi = count `in` (giriş), B→A = `out`. (_side işareti: B-yanı pozitif, count "in"=pozitife geçiş.)
- **line_override gerçekten uygulanıyor**: dikey çizgi (1/1) vs yatay çizgi (0/7) farklı sonuç → kaydedilen çizgi kullanılıyor.
- **İç tutarlılık**: olay sayısı == giriş+çıkış; her olayın track_id/yön/ts geçerli; her track tek sayılıyor (mükerrer yok).
- Dürüst sınır: gerçek-dünya **doğruluğu** çizgi konumu + conf + model (yol11n) ile kalibre edilmeli. Plaka OCR gürültüsü (ZI6618/E26618) → çok-kareli oylama gerekiyor (backlog).

## Yapıldı — watchlist eşleşme alarmı
- 💾 `store.py`: `alerts` tablosu + `match_plates()` (read ∩ watch_plates) + `add_alert`/`recent_alerts`.
- ⚡ `server.py`: `/api/run` plaka kolu okunan plakaları kara listeyle eşleştirir → alert yazar + summary.plate.alerts döner. Yeni `/api/alerts`.
- 🎨 `web/index.html`: Olaylar ekranında kırmızı **Uyarılar bandı** (sayaç + blacklist chip); Test plaka kartında eşleşme uyarısı.

## Doğrulama
- Yön + tutarlılık testi: ✅ (yukarıda)
- Uçtan uca: `EBC529` kara listeye eklendi → otopark plaka koşusu → alert üretildi → `/api/alerts` döndü → Olaylar'da kırmızı band görüldü (screenshot) ✅

## Veri nerede? (mimari cevap)
- **Depo**: tek SQLite dosyası `output/videoai.db`. Tablolar: runs, count_events, plate_reads, face_events, zones, watch_plates, watch_faces, alerts. Her kayıt timestamp'li; events `synced` bayraklı (offline→merkez senkron için).
- **KVKK**: ham kare/video saklanmaz → sadece metrik/metin/embedding. Annotated video `output/` (repoya girmez), `/media` ile sunulur.
- **Üretim**: saha başına yerel SQLite → internet gelince merkez Postgres/pgvector'a `synced` ile push (store-and-forward).
- **Gösterim**: web UI — Canlı (snapshot), Test (metrik+video), Olaylar (birleşik akış + kırmızı uyarı bandı), Listeler (izleme listesi), Bölgeler (zones).

## Değişen
- `src/store.py` (alerts + match), `src/server.py` (/api/run eşleşme + /api/alerts), `web/index.html` (uyarı bandı)

## Sonraki Adım
- [ ] Çok-kareli plaka oylama (OCR gürültüsü → doğruluk)
- [ ] Yüz watchlist eşleşmesi (embedding cosine) — KVKK kapısıyla
- [ ] Canlı tile otomatik yenileme · uzun video arka plan job
