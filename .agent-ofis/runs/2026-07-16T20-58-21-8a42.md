---
title: "Run — Test iptal mekanizması + yüz re-ID (46→13) + yeni yüz test videoları"
type: run
run_id: 2026-07-16T20-58-21-8a42
date: 2026-07-16
topics: [yüz, reid, ui, poc]
tags: [run/completed, yüz, ui]
aliases: [test-iptal, yuz-reid-dedup]
related_runs: [[runs/2026-07-09T23-50-00-matrix]]
---

# Run — 2026-07-16 — Test iptal + yüz re-ID düzeltmesi

**İstek:** (1) "Testte çalıştıra bastığımda önceki test iptal olsun" (2) "Yüz tanımada 5 kişi var, kasa önünde 25-35 gibi davranıyor — modele bak" (3) "Yüz için daha iyi test videosu bul".

## Kök nedenler
- İptal: `RUN_LOCK` tek analize izin veriyordu, iptal mekanizması hiç yoktu — yeni job "sırada bekliyor"a düşüyor, eski analiz sonuna dek koşuyordu. UI'da da iki poll döngüsü aynı paneli eziyordu.
- Yüz sayısı: `detections` = kapanan TRACK sayısıydı, kişi değil. Takipçi salt konumsal (IoU+merkez), miss ~1 sn → kasada bekleyen kişinin yüzü kaçınca track kopup aynı kişi yeniden sayılıyordu. ArcFace embedding'i vardı ama kimlik birleştirmede kullanılmıyordu.

## Yapılan
- ⚡ server.py: job'lara `cancel` Event; POST /api/run önceki running job'ları iptal eder; DELETE /api/run/{id}; `should_stop` count/plate/face'e geçirilir; iptal edilen job webify'ı atlar. Status sözleşmesi: running|cancelling|cancelled|done|error, stage "iptal edildi".
- ⚡ face.py: `_best_reid` (gallery'den embedding'le track diriltme) + `_dedup_tracks` (finalize-öncesi greedy embedding birleştirme) + `min_track_frames` hayalet elemesi. Eşikler config: `face.reid_threshold: 0.40`, `face.min_track_frames: 3`.
- 🎨 index.html: `_runSeq` stale-loop koruması + `cancelled` işleme ("✕ iptal edildi").
- 🎯 Yeni yüz test videoları (Intel sample-videos, tam okunabilir): `face-demographics-walking-and-pause.mp4` (91sn), `head-pose-face-detection-female-and-male.mp4` (143sn) → kameralar: `yuz-testi-yuruyus`, `yuz-testi-portre` (task: face).

## Ölçümler
- Kasa Önü (store-aisle): baseline **46** → re-ID **18** → +dedup **13** kişi (raw 910 sabit — detektör davranışı değişmedi). Gerçek ~5-6; kalibrasyon açık konu (eşik 0.35 → ~9-10 tahmini, yanlış birleşme riski artar).
- Yüz Testi — Yürüyüş: 9 kişi (M5/K4, ort. yaş ~44), video üretildi.
- E2E iptal: analiz ortasında yeni Çalıştır → önceki job **0.55 sn'de cancelled**; devralan job sorunsuz tamamlandı.

## Doğrulama
- compileall ✅ · pytest **27 passed** (yeni: TestYuzReid, TestYuzDedup) ✅ · smoke_run (cli --help) ✅ · E2E iptal ✅ · bağımsız diff denetimi: bu run kapanışında koşuldu (sonucu rapora işlendi).
- Sunucu taze kodla yeniden başlatıldı (eski süreç zaten ölmüştü — "sistem çalışmıyor" şikâyetinin bir bileşeni).

## Bağımsız denetim (judge-pass)
- CRITICAL yok. 2 MEDIUM bulundu ve AYNI RUN'DA kapatıldı: (1) cancelling/done yarışı → `JOBS_STATE_LOCK` ile terminal yazımlar + check-and-set kilitlendi; (2) gallery sınırsız büyüme → hayaletler gallery dışı + `face.gallery_max: 128` + `_compact_gallery` (dedup-kompakt + retired). /api/counts `active` cancelling'i de sayıyor. Son ölçüm: Kasa 12 (band içi), pytest **30 passed**.
- Kalan LOW'lar (bilinçli, açık konu): iptal edilen koşunun kısmi olayları bir sonraki koşunun `clear_analysis`'ine dek görünür; webify'sız yetim mp4 (aynı stem'e üzerine yazılır); POST /run ağ hatasında eski job UI'siz koşabilir (sonraki Çalıştır düzeltir).

## Paralel oturum notu
- Aynı working tree'de eşzamanlı ikinci oturum (run [[runs/2026-07-16T20-52-00-c4e1]]) aynı iptal işini uyguladı + Durdur butonu ekledi + sunucuyu ayrıca restart etti. Nihai durum birleşik ve E2E doğrulanmış; `cancelled=True` alanı ve Durdur korunarak üzerine inşa edildi. Ders: aynı repoda tek oturum.

## Açık konular
- reid_threshold kalibrasyonu (0.40 → 13 kişi; gerçek 5-6 için 0.35 denenebilir, riskli).
- Abide kameralarında sayım çizgileri hâlâ çizilmemiş (9 Temmuz DB kaybı) — çizgisiz kamerada count anında "atlandı" döner.
- Canlı panelde male/female/avg_age artık analiz sonunda dolar (finalize sona taşındı).
