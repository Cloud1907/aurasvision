---
title: "Run — Test iptal mekanizması: preempt + Durdur butonu + DELETE /api/run"
type: run
run_id: 2026-07-16T20-52-00-c4e1
date: 2026-07-16
topics: [ui, sayma, plaka]
tags: [run/completed, ui]
aliases: [test-iptal, durdur-butonu, preempt]
related_runs: [[runs/2026-07-09T23-50-00-matrix]]
---

# Run — 2026-07-16 — Test iptal mekanizması

**İstek:** "sistemi ayağa kaldır + testte çalıştıra bastığımda önceki test iptal olsun."

## Kök neden
- `RUN_LOCK` (server.py) tek analize izin veriyor; ikinci Çalıştır yeni job'ı KUYRUĞA sokuyordu ("sırada bekliyor"), eski test videoyu sonuna kadar işliyordu. Kodda iptal mekanizması hiç yoktu (ne server ne count/plate/face).

## Yapılan
- `run_count/run_plate/run_face` → `should_stop: Callable` parametresi; kare döngüsü başında kontrol, temiz `break` (writer finalize korunur, exception yok).
- server.py: job başına `threading.Event` (JSON'dan `pop` edilir); `POST /api/run` preempt (koşan job'lar iptale çekilir, ara durum `cancelling`); modüller arası iptal kontrol noktaları (kalan modüller + `_webify` atlanır); `DELETE /api/run/{id}` (idempotent, 404); iptal bitişi `status=cancelled, cancelled=True, stage="iptal edildi"`.
- web/index.html: `_runSeq` + `currentJobId` ile tek-sahip poll (bayat döngü sessiz ölür); "Durdur" butonu (yalnız koşarken görünür, DELETE çağırır); cancelled için nötr amber bant ("Test durduruldu" / "Test iptal edildi — yeni test başlatıldı") — error kırmızısı değil.
- Sunucu taze kodla yeniden başlatıldı (eski 6.5 günlük süreç kapatıldı — [[runs/2026-07-09T11-40-00-live]] dersi uygulandı).

## Doğrulama (canlı E2E, curl)
- compileall ✅ · pytest 24/24 ✅ · import smoke ✅
- Preempt: job1 (plaka) koşarken job2 POST → job1 **13 ms'de cancelled**, job2 beklemeden başladı ✅
- Durdur: koşan job'a DELETE → **346 ms'de cancelled**, `cancelled: true` ✅
- İdempotent DELETE ✅ · olmayan job 404 ✅ · sonrası `active: false` ✅

## Not
- face.py'de bu run'a ait OLMAYAN commit'lenmemiş re-id/gallery değişiklikleri working tree'de duruyor (10 Temmuz kalıntısı) — commit kararı kullanıcıda.
- worker/ingestor süreçlerine dokunulmadı (modül imzaları geriye uyumlu, keyword default None).
