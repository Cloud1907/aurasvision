---
title: "Run — Web arayüzü: FastAPI + canvas bölge editörü"
type: run
status: completed
date: 2026-06-29
run_id: 2026-06-29T21-26-23-fc10
topics: [ui, sayma, plaka, retail, poc]
tags: [run/completed]
related_decisions: [[decisions/0001-yigin-ve-mimari]]
aliases: [run-2026-06-29-webui]
---

# Run — Web arayüzü (maket → gerçek kod)

## İstek
"başla bakalım" → maketteki 5 ekranlı arayüzü gerçek koda çevir (FastAPI + canvas editör).

## Yapıldı
- ⚡ Backend — `src/server.py` (FastAPI): /api/cameras, /api/snapshot (anlık JPEG, diske yazmaz), /api/zones GET+POST, /api/events (birleşik akış), /api/lists CRUD. `store.py`'a zones + watch_plates + watch_faces tabloları + metodlar. `config.yaml`'a cameras + server bloğu.
- 🎨 Frontend — `web/index.html`: vanilla SPA, 5 ekran (Canlı/Kameralar/Bölgeler/Olaylar/Listeler). Canvas bölge editörü: gerçek snapshot üzerine çizgi (2 nokta) / poligon (bölge/ihlal), normalize 0-1 koordinat, kaydet.
- 🔍 QA — 6 endpoint curl yeşil; Claude_Preview ile UI screenshot (canlı snapshot'lar, canvas editör, listeler) doğrulandı.

## Doğrulama
- syntax_check (py_compile): ✅ · smoke_run (uvicorn ayağa kalkıyor): ✅
- API: /cameras 3 kamera, /snapshot JPEG 27KB, /events DB'den, /lists ekle+oku, /zones kaydet+oku — hepsi 200 ✅
- UI screenshot: Canlı (3 gerçek snapshot), Bölgeler (canvas + çizgi+→IN + araç paneli), Listeler (sekme+form+tablo) ✅

## Öğrenilen / Notlar
- Snapshot ortadaki kareyi alır (daha temsili), diske yazılmaz (KVKK forbidden uyumu).
- Editör "tam durum" gönderir → POST /api/zones önce clear_zones, sonra ekler (idempotent kaydet).
- Bölge editörü araç paneli dar ekranda taşıyordu → canvas `flex:1 1 420px` + container `flex-wrap` ile çözüldü.
- chrome-devtools MCP başka profile kilitli → Claude_Preview (launch.json) ile screenshot alındı.

## Değişen / Eklenen
- `src/server.py` (yeni), `src/store.py` (+zones/watchlist), `config.yaml` (+cameras/server)
- `web/index.html` (yeni SPA), `.claude/launch.json` (preview), `requirements.txt` (+fastapi/uvicorn)

## Sonraki Adım
- [ ] Editör çizgisini `count.py`'a bağla (kaydedilen line → gerçek sayım kullansın)
- [ ] Olay akışına kara liste eşleşme uyarısı (plaka/yüz watchlist match → kırmızı alarm)
- [ ] Kamera ekleme/silme UI'dan (şimdilik config.yaml)
- [ ] Canlı görünümde periyodik snapshot yenileme (MJPEG/poll)
