<!-- GENERATED by bin/ofis-sync — DO NOT EDIT. Kaynak: projects/videoai.yml -->

# AGENTS.md — AurasVision — Görüntü Analitiği Platformu

> Bu dosya manifest'ten ÜRETİLDİ (`bin/ofis-sync`). Elle düzenleme — manifest'i düzenle, sync'i koş.
> Enforcement modu: **gradual** · Etiket: enforced-deterministic (ölçülebilen) + governed-agentic (yorumsal).

## Tech Stack
- backend: python 3.11
- frontend: vanilla
- database: sqlite
- test: pytest

## Komutlar
- `build`: `source .venv/bin/activate && find src -name '*.py' -print0 | xargs -0 python -m py_compile`
- `run`: `source .venv/bin/activate && python -m src.cli analyze --source <video>`
- `test`: `source .venv/bin/activate && pytest -q`

## ENFORCED Kurallar (blok — geçilmeden merge yok)
### Pazarlıksız yasaklar (forbidden)
- İsimli yüz tanımayı KVKK açık rıza / aydınlatma metni olmadan üretim akışına koymak
- Ham yüz görüntüsünü / video karesini gereksiz kalıcı saklamak — embedding + anonim metrik tercih et
- Bağlantı string'i, API anahtarı, kişisel veri hardcode etmek (.env + secret yönetimi)
- Liveness/anti-spoof olmadan erişim kontrolü (kapı açma) akışı kurmak
- torch/onnx modelini her karede yeniden yüklemek — model bir kez yüklenir, döngüde reuse
- Mesafe/benzerlik eşiğini koda sihirli sabit olarak gömmek — eşikler config'ten gelir
- .venv / model ağırlıkları (*.pt, *.onnx) / video dosyalarını repoya commit etmek

### CI Required Checks (branch protection)
- typecheck · lint · test · build

## ADVISORY Kurallar (uyarı — bloklamaz)
- Tek giriş noktası: src/cli.py — alt komutlar: count | face | plate | analyze
- Modeller bir kez yüklenir (lazy singleton), cihaz seçimi: MPS > CUDA > CPU otomatik
- Sonuçlar SQLite'a yazılır; her kayıtta kamera_id + timestamp; senkron için synced bayrağı
- Eşik/parametre değerleri config (yaml/env) üzerinden; kodda magic number yok
- Tip ipuçları (type hints) + docstring; modüller src/ altında (detect, track, face, plate, store, viz)
- Retail analitiğinde varsayılan anonim: kimliklendirme opt-in ve KVKK kapısına bağlı

## Acceptance (nihai onay)
- type: **none**

## Definition of Done
- required CI yeşil · gate'ler approved · render varsa approver gördü · waiver yok · run-log yazıldı

---
*Tek kaynak: `projects/videoai.yml`. Drift varsa `ofis-sync --check` CI'da fail eder.*
