# GENERATED by ofis-qa · api/schemathesis · template_version 0.2.0 · videoai
# API kontrat kapısı — OpenAPI spec'inden otomatik fuzzing.
name: qa-api-contract
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install "schemathesis==4.*"
      # NOT: servisi burada ayağa kaldır (docker compose up -d) ya da QA_API_URL preview'a baksın.
      - name: kontrat testi
        run: schemathesis run "${QA_OPENAPI:-openapi.json}" --url "${QA_API_URL:-http://localhost:5000}" --checks all
