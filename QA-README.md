# QA — Web E2E (Playwright) · ofis-qa

> GENERATED by ofis-qa · web/default · template_version 0.2.0 · AurasVision — Görüntü Analitiği Platformu
> Sınır (ADR-0012): test kodu bu repo'da, bu repo'nun CI'ında koşar.

## Kur & koş
```bash
npm i -D @playwright/test@1.61.1 allure-playwright@3.4.1
npx playwright install --with-deps chromium
QA_BASE_URL=http://localhost:3000 npx playwright test
npx playwright show-report
```
Dosya sahipliği: `playwright.config.ts` + workflow = **ofis-qa** (upgrade değiştirebilir) · `e2e/*.spec.ts` = **SEN**.
